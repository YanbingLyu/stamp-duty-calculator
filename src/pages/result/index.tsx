import React from 'react';
// import { SessionRefresh, SessionRedirect, IsAuthenticated } from '@lendi/lala-react';
import { Layout } from '../../layout';

const StampDutyResultPage: React.FC = () => {
  return (
    // <SessionRefresh>
    //   <SessionRedirect returnURL={window.location.href}>
    //     <IsAuthenticated>
    //       <Layout>Result page</Layout>
    //     </IsAuthenticated>
    //   </SessionRedirect>
    // </SessionRefresh>
    <Layout>Result page</Layout>
  );
};

export default StampDutyResultPage;
